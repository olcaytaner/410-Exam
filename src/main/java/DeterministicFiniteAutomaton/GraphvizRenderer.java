package DeterministicFiniteAutomaton;

// Disabled for now

//import guru.nidi.graphviz.engine.Format;
//import guru.nidi.graphviz.engine.Graphviz;
//import guru.nidi.graphviz.model.Graph;
//import guru.nidi.graphviz.parse.Parser;

import java.io.ByteArrayInputStream;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;

public class GraphvizRenderer {
//  public static void renderToSvg(String dotSource, String outputPath) throws IOException {
//    try (InputStream dot = new ByteArrayInputStream(dotSource.getBytes())) {
//      Parser parser = new Parser();
//      Graph g = parser.read(dot).toImmutable();
//
//      Graphviz.fromGraph(g)
//        .render(Format.SVG)
//        .toFile(new File(outputPath));
//    }
//  }
}
